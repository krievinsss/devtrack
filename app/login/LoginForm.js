'use client';
import { useState } from 'react';import { useRouter } from 'next/navigation';import { ArrowRight, Github, Sparkles } from 'lucide-react';
export default function LoginForm(){const r=useRouter();const [email,setEmail]=useState('teacher@devtrack.local');const [password,setPassword]=useState('Demo123!');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');const res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const d=await res.json();setBusy(false);if(!res.ok)return setError(d.error||'Login failed');r.push('/dashboard');r.refresh()}return <form onSubmit={submit} className="login-card"><div className="login-logo">D</div><div><span className="eyebrow">DEVTRACK</span><h1>Developer progress, clearly.</h1><p>Git activity, code quality, feedback and assessment in one professional workspace.</p></div><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></label>{error&&<div className="form-error">{error}</div>}<button className="btn primary wide" disabled={busy}>{busy?'Signing in…':'Sign in'}<ArrowRight size={17}/></button><div className="demo-users"><span>Demo accounts</span><button type="button" onClick={()=>setEmail('teacher@devtrack.local')}>Teacher</button><button type="button" onClick={()=>setEmail('janis@devtrack.local')}>Student</button></div><div className="login-foot"><span><Github size={15}/> GitHub App ready</span><span><Sparkles size={15}/> AI review ready</span></div></form>}
