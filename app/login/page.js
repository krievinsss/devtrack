import { redirect } from 'next/navigation';import { currentUser } from '@/lib/auth';import LoginForm from './LoginForm';
export default async function Login(){if(await currentUser())redirect('/dashboard');return <main className="login-page"><div className="login-visual"><div className="visual-copy"><span className="eyebrow">FROM STUDENT WORK TO DEVELOPER GROWTH</span><h2>See the work behind the grade.</h2><p>Track progress across projects, attendance and teacher feedback — without turning the platform into another school journal.</p></div></div><LoginForm/></main>}
