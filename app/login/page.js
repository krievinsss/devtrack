import { redirect } from 'next/navigation';import { currentUser } from '@/lib/auth';import LoginForm from './LoginForm';
export default async function Login(){if(await currentUser())redirect('/dashboard');return <main className="login-page"><div className="login-visual"><div className="visual-copy"><span className="eyebrow">FROM STUDENT CODE TO DEVELOPER GROWTH</span><h2>See the work behind the grade.</h2><p>Track progress across projects, commits, attendance and teacher feedback — without turning the platform into another school journal.</p><div className="visual-metrics"><div><b>386</b><span>commits tracked</span></div><div><b>87%</b><span>attendance</span></div><div><b>7.4</b><span>average score</span></div></div></div></div><LoginForm/></main>}
