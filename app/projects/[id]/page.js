import { notFound } from 'next/navigation';import AppShell from '@/components/AppShell';import ProjectWorkspace from '@/components/ProjectWorkspace';import { requirePageUser } from '@/lib/page';import { canAccessStudent } from '@/lib/auth';import { getProject } from '@/services/projects';import { getUser } from '@/services/users';import { readJson } from '@/lib/storage';import { getFeedback } from '@/services/feedback';import { getAssessment } from '@/services/assessments';import { repoFile,repoTree } from '@/services/github';import { weeklySeries,contributionDays } from '@/services/analytics';
export default async function Project({params}){const user=await requirePageUser();const {id}=await params;const project=await getProject(id);if(!project||!canAccessStudent(user,project.studentId))notFound();const student=await getUser(project.studentId);const commits=(await readJson('commits',[])).filter(c=>c.repositoryId===id).sort((a,b)=>new Date(b.timestamp)-new Date(a.timestamp));const feedback=await getFeedback(id);const assessment=await getAssessment(id);const review=(await readJson('aiReviews',[])).filter(r=>r.projectId===id).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt))[0]||null;let tree=[];try{tree=await repoTree(id,'')}catch{}let fileEntry=tree.find(x=>x.type==='blob');let defaultFile=null;if(fileEntry){try{defaultFile={path:fileEntry.path,...await repoFile(id,fileEntry.path)}}catch{}}return <AppShell user={user}><ProjectWorkspace project={project} student={student} commits={commits} feedback={feedback} assessment={assessment} review={review} tree={tree} defaultFile={defaultFile} series={weeklySeries(commits)} heatmap={contributionDays(commits)} user={user}/></AppShell>}
