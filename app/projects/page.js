import Link from 'next/link';import AppShell from '@/components/AppShell';import { PageHeader,Badge,Progress } from '@/components/UI';import { requirePageUser } from '@/lib/page';import { getProjects } from '@/services/projects';import { getUsers } from '@/services/users';
export default async function Projects(){const user=await requirePageUser();const users=await getUsers();let projects=await getProjects();if(user.role==='student')projects=projects.filter(p=>p.studentId===user.id);return <AppShell user={user}><PageHeader eyebrow="Repository workspace" title={user.role==='student'?'My projects':'Projects'} description="Code, Git history, progress, reviews and assessment."/><div className="project-grid">{projects.map(p=>{const s=users.find(u=>u.id===p.studentId);return <Link href={`/projects/${p.id}`} className="panel project-list-card" key={p.id}><div className="panel-title"><div><Badge tone="purple">{p.status}</Badge><h3>{p.name}</h3></div><span>{p.progress}%</span></div><p>{s?.firstName} {s?.lastName} · {p.technologies.join(' · ')}</p><Progress value={p.progress}/><div className="project-meta"><span>{p.githubRepo?`${p.githubOwner}/${p.githubRepo}`:'Demo repository'}</span><span>AI {p.aiScore}/10</span><span>Teacher {p.teacherScore}/10</span></div></Link>})}</div></AppShell>}
