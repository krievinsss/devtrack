'use client';
import { Trophy,Lock,Sparkles } from 'lucide-react';
export default function AchievementsGrid({achievements,unlocked=[]}){const owned=new Set(unlocked);const done=achievements.filter(a=>owned.has(a.id)).length;return <div className="achievements-page"><section className="achievement-hero"><div><span className="eyebrow">ACHIEVEMENT COLLECTION</span><h1>Your developer trophy room.</h1><p>Complete projects, earn strong grades and level up to unlock the full collection.</p></div><div className="achievement-total"><Trophy size={22}/><strong>{done}/{achievements.length}</strong><span>unlocked</span></div></section><div className="achievement-progress"><span style={{width:`${achievements.length?done/achievements.length*100:0}%`}}/></div><div className="achievement-grid">{achievements.map((a,index)=>{const isUnlocked=owned.has(a.id);return <article className={`achievement-card ${isUnlocked?'unlocked':'locked'}`} key={a.id}><div className="achievement-icon">{isUnlocked?<Trophy size={28}/>:<Lock size={24}/>}</div><div className="achievement-number">#{String(index+1).padStart(2,'0')}</div><h3>{a.name}</h3><p>{a.description}</p><span className="achievement-state">{isUnlocked?<><Sparkles size={13}/> Unlocked</>:<><Lock size={12}/> Locked</>}</span></article>})}</div></div>}
