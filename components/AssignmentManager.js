'use client';
import { useState } from 'react';
import RichTextEditor from './RichTextEditor';
import { richTextToPlain } from '@/lib/richText';
export default function AssignmentManager({groups}){const [title,setTitle]=useState(''),[groupId,setGroupId]=useState(groups[0]?.id||''),[descriptionHtml,setDescriptionHtml]=useState('<h2>Projekta mērķis</h2><p>Apraksti, kas studentam jāizstrādā un kāds ir sagaidāmais rezultāts.</p>'),[requirements,setRequirements]=useState(''),[technologies,setTechnologies]=useState('JavaScript, API, CSS'),[startDate,setStartDate]=useState(new Date().toISOString().slice(0,10)),[deadline,setDeadline]=useState(''),[rubric,setRubric]=useState([{name:'Funkcionalitāte',max:5},{name:'Koda kvalitāte',max:5},{name:'Git workflow',max:3}]),[busy,setBusy]=useState(false);
const setCriterion=(i,key,value)=>setRubric(rubric.map((r,j)=>j===i?{...r,[key]:key==='max'?Number(value):value}:r));
async function create(){setBusy(true);const body={title,groupId,description:richTextToPlain(descriptionHtml),descriptionHtml,requirements:requirements.split('\n').map(x=>x.trim()).filter(Boolean),technologies:technologies.split(',').map(x=>x.trim()).filter(Boolean),rubric,startDate,deadline};const r=await fetch('/api/assignments',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});setBusy(false);if(r.ok)location.reload();else alert((await r.json()).error)}
return <section className="panel"><div className="panel-title"><h3>New group project</h3><span>Publish once → project for every student</span></div><div className="form-row"><input placeholder="Project title, e.g. Weather App" value={title} onChange={e=>setTitle(e.target.value)}/><select value={groupId} onChange={e=>setGroupId(e.target.value)}>{groups.map(g=><option key={g.id} value={g.id}>{g.name}</option>)}</select><div><label className="field-label">Assignment page</label><RichTextEditor value={descriptionHtml} onChange={setDescriptionHtml} placeholder="Create a clear, formatted project brief…"/></div><textarea placeholder={'Requirements — one per line\nWeather API\nError handling\nResponsive UI'} value={requirements} onChange={e=>setRequirements(e.target.value)}/><input placeholder="Technologies, comma separated" value={technologies} onChange={e=>setTechnologies(e.target.value)}/><div className="inline-row"><label>Start <input type="date" value={startDate} onChange={e=>setStartDate(e.target.value)}/></label><label>Deadline <input type="date" value={deadline} onChange={e=>setDeadline(e.target.value)}/></label></div><h4>Final assessment criteria</h4>{rubric.map((r,i)=><div className="inline-row" key={i}><input value={r.name} onChange={e=>setCriterion(i,'name',e.target.value)}/><input type="number" min="1" value={r.max} onChange={e=>setCriterion(i,'max',e.target.value)}/><button className="btn secondary" onClick={()=>setRubric(rubric.filter((_,j)=>j!==i))}>Remove</button></div>)}<button className="btn secondary" onClick={()=>setRubric([...rubric,{name:'',max:1}])}>+ Criterion</button><button className="btn primary" disabled={busy||!title||!deadline||!groupId||!richTextToPlain(descriptionHtml)} onClick={create}>{busy?'Publishing…':'Publish to group'}</button></div></section>}
