'use client';
import { Trophy,Sparkles,Users,Flame } from 'lucide-react';

export default function ClassmatesGrid({groupName,classmates,currentUserId}){
  return <div className="classmates-page"><section className="classmates-hero"><div><span className="eyebrow">CLASS SHOWCASE</span><h1>{groupName||'Classmates'}</h1><p>Level up, collect cosmetics and build your DevTrack identity. Grades stay private — only game progress and equipped cosmetics are shown here.</p></div><div className="classmates-summary"><Users size={20}/><b>{classmates.length}</b><span>developers</span></div></section><div className="classmates-grid">{classmates.map((c,index)=><article className={`classmate-card ${c.studentId===currentUserId?'is-you':''}`} key={c.studentId}><div className="classmate-card-top"><span className="class-level"><Flame size={13}/> Level {c.level}</span>{c.studentId===currentUserId&&<span className="you-chip">You</span>}</div><div className={`classmate-avatar ${c.frameClass||''}`}>{c.initials}</div><div className="classmate-identity"><h3>{c.name}</h3><span>{c.title||'Student Developer'}</span>{c.slug&&<b>#{c.slug}</b>}</div><div className="classmate-xp"><div><span>XP</span><b>{c.xp}</b></div><div className="classmate-xpbar"><span style={{width:`${c.levelPct}%`}}/></div><small>{c.nextXp} XP to level {c.level+1}</small></div><div className="classmate-foot"><span><Trophy size={13}/>{c.achievementCount} achievements</span>{c.themeName&&<span><Sparkles size={13}/>{c.themeName}</span>}</div></article>)}</div></div>
}
